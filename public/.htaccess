# Apache .htaccess for multiprompt/public/
# All requests go through index.php (SPA + API routing)

Options -Indexes
RewriteEngine On

# Serve static assets directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route everything else to index.php
RewriteRule ^ index.php [L]
